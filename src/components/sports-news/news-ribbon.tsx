import Container from '@mui/material/Container'

export default function NewsRibbon() {
    return (
        <>
            <Container  id="news-ribbon-container" sx={{ my:2, display: 'flex', justifyContent: 'center', border:'1px dashed' }}>
                News Ribbon
            </Container>
        </>
    )
}